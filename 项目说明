项目是用Vue + axios + element ui + php +mysql 搭建的 
后台的接口是 http://127.0.0.1:8080/blogs/ 下面的 
后台服务器端口必须是 8080 ！！！后台的用户名 + 密码 都是root 

后台的数据库必须是blogs 然后导入我给的sql文件就可 

访问的话 只需访问 dist 打包文件即可

后台做了简单的登录 拦截 账号 是admin 密码是  123456

文件名blogs-php 要改为blogs 然后放到服务器下面即可
